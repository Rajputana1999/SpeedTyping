$(document).ready(function(){ /// This should be inside  #end.click func../
    let input = $('#inputText');
    let wpm = input.val().length/(5*((Ttime-time)/60));
    let finalError = 0;
    for(let i=0; i<input.length; i++){
        if(input[i]!=text[i])finalError++;
    }
    let accuracy ;
    
});